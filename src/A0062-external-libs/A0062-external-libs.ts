// when install a lib, install the lib and '@types/lib -D'
import validator from 'validator';
import _ from 'lodash';

console.log(validator.isEmail('guilhermesenna_16@hotmail.com'));
console.log(_.clone([1, 2, 3, 4, 5]));
